<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/shuffle_item"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:background="@drawable/selector_shuffle_light"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <TextView
        android:text="@string/shuffle"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_centerVertical="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@+id/shuffle_icon"
        android:layout_toLeftOf="@+id/shuffle_icon"/>

    <ImageView
        android:id="@+id/shuffle_icon"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_margin="5dp"
        android:layout_width="?android:attr/listPreferredItemHeight"
        android:layout_height="match_parent"
        android:src="@drawable/ic_shuffle_white"
        android:contentDescription="@string/shuffle" />

</RelativeLayout>